{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Bobiverse replicant genealogy",
  "type": "object",
  "required": ["bobs"],
  "properties": {
    "bobs": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name", "src"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9_]+$",
            "description": "Stable internal key. Never displayed; safe to keep when a name is corrected."
          },
          "name": {
            "type": "string",
            "description": "Display name as spelled in the primary text where possible."
          },
          "alias": {
            "type": "string",
            "description": "The name this Bob left behind. `name` is what he goes by now, `alias` is what he was called before — Will was Riker, Morlock was Jeremy. Recorded rather than overwritten so both stay searchable, and so the id can stay stable when other records point at it as a parent."
          },
          "parent": {
            "type": "string",
            "description": "id of the source Bob. Omit entirely for tiers C and X, which have no ancestor on record. For tier P this is an ancestor, not necessarily a direct parent."
          },
          "src": {
            "type": "string",
            "enum": ["o", "t", "p", "c", "x"],
            "description": "How well the BOOKS establish this Bob's parentage. They are the only source: Taylor's 2017 genealogy and the fandom wiki were dropped, because a claim we cannot point at a page for should not be asserted. o=the original, Bob-1, who has no parent to find; t=parent stated in the books, narrative or the Cast of Characters appendix; p=an ancestor is stated but the generations between are not; c=no ancestor on record; x=the record was deliberately expunged. Tier says nothing about how well documented a Bob is otherwise — Herschel is tier C with eighteen POV chapters and a generation he states out loud. Tier X is the narrow, stronger case: we know why the data is missing, because somebody removed it on purpose; it requires a cite and a partialNote."
          },
          "cite": {
            "type": "string",
            "description": "Chapter reference for whatever this record's strongest textual claim is. Required for tier T, where it must support the parent link. Welcome on any tier \u2014 a tier C Bob may still have a citable generation, death, or first appearance. Format: 'Bk<n> ch<n> \u00b7 <POV>, <date>'."
          },
          "gen": {
            "type": "integer",
            "minimum": 1,
            "description": "Store this ONLY where the books state it out loud and the tree cannot work it out — currently three records. Everywhere else the console counts it down the parent chain, so the two can never drift apart. The validator errors if a stored gen disagrees with tree depth."
          },
          "desig": { "type": "string", "description": "In-universe designation, and the Bob's actual identity — they all wear the same face, so Guppy identifies them by serial number or not at all (Bk3 ch21). Two schemes: 'Bob-<n>' for the early global numbering, abandoned once coordinating numbers between star systems got unwieldy (Bk1 ch28); and 'HIC<catalogue>-<n>' after, where the Hipparcos Catalog number names the system the Bob was built in and <n> is the index within it (Bk1 ch15). The validator checks HIC prefixes against origin." },
          "vessel": { "type": "string", "description": "The hull the Bob's matrix flies in, e.g. 'Heaven-2'. Only values printed verbatim in the books — three pattern-extrapolated ones were swept out." },
          "born": { "type": "string", "description": "Activation date, e.g. 'Sep 2205'. Only kept where a chapter of that date names the Bob; dates inherited from the dropped sources were swept out rather than left looking like findings." },
          "origin": { "type": "string", "description": "id of the system where this Bob was built, keyed into data/systems.json. The validator rejects an id that isn't there." },
          "visited": { "type": "array", "items": { "type": "string" }, "description": "System ids, keyed into data/systems.json. Was a comma-separated string; became a list when the systems file gave the ids somewhere to point." },
          "status": { "type": "string", "enum": ["active", "lost", "unknown"] },
          "lostAt": { "type": "string", "description": "id of the system where this Bob was destroyed or last known, when status is lost. Places that aren't systems don't belong here — Bender's old value was the phrase \"missing, later located\", which is a note, not a location." },
          "faction": {
            "type": "string",
            "enum": ["Starfleet", "Skippies", "Gamers", "Borg"],
            "description": "Affiliation, not lineage. The four mutual-interest groups Bill describes as forming once replicative drift made Bobs into 'joiners'. Deliberately separate from the tree because factions cut across branches — Starfleet took joiners from other lines and purged its own genealogy records, so it can never be drawn as a subtree."
          },
          "ref": { "type": "string", "description": "Namesake. Reader inference, not canon." },
          "conflict": { "type": "string", "description": "Describe the disagreement between sources and which one the tree follows." },
          "partialNote": { "type": "string", "description": "Required for tiers P and X. For P, exactly what the books do and don't say about the gap. For X, who removed the record and how we know." },
          "priorClaim": { "type": "string", "description": "A parentage claim from a source we dropped — Taylor's 2017 tree or the fandom wiki. Recorded as a research lead so the work isn't lost, and deliberately NOT a parent link: the tree only draws what the books support. Delete it if the books ever settle the question." },
          "note": { "type": "string", "description": "Our prose, shown in the dossier under ANNOTATION and deliberately unbracketed — in the books, square brackets mark machine output, so the registry's own fields are bracketed and this is not." }
        }
      }
    }
  }
}
